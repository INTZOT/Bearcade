# SND5《剑与消亡V：上密会战》游戏开发文档
### Sword & Dissolution V : Battle of Upper Mysiaen · 完整设计与开发规范（重建提示词版）

> 版本：2026-08-14 重建版
> 适用平台：Minecraft 基岩版 1.26.40（Windows / 手机均可）
> 包类型：行为包 + 资源包 + 脚本，最终交付 `.mcaddon`
> 文档定位：本文件汇总了 SND5 项目全部已确认的设计、流程、数值、技术决策与开发规范，**作为提示词用于从零重新生成一套符合正常开发流程的 SND5**。

---

## 0. 如何使用本文档（提示词说明）

把本文档全文作为提示词交给开发环境（Codex / 其他 AI 或开发者）时，请附带以下约束：

1. **从零重建，不做历史代码修补**。忽略旧的 `main.js` 实现细节与临时方案（如末影水晶阵眼、自定义模型、原版狼反复切换等历史遗留），只按本文档描述的最终设计重新实现。
2. **必须遵守本文档第 5 章“技术开发规范”**：目录结构、manifest、脚本工程规范、API 兼容性铁律、校验/打包/同步流程均按规范执行。
3. **数值以第 4 章“游戏细节”为准**；未决事项与后续规划见第 7 章。
4. **开发完成后按第 6 章“验收标准”逐项回归**，并在项目根目录维护交接文档与使用手册。
5. 命名空间沿用 `jimie:`（可全局替换为项目正式命名空间，但必须全工程一致）；学派 ID 沿用 `jiemie / fuzu / life / speed`，圣典 ID 沿用 `buddha / priest / knight / doctor / summoner / blade / herbalist / prophet / formation`。

---

## 1. 项目概述

### 1.1 基本信息

| 项目 | 内容 |
|---|---|
| 游戏名称 | 剑与消亡V：上密会战（Sword & Dissolution V : Battle of Upper Mysiaen，简称 SND5） |
| 游戏类型 | Minecraft 基岩版 PvP 对战 Addon（“学派战技能包”） |
| 目标版本 | 基岩版 1.26.40 正式版 |
| 脚本 API | `@minecraft/server` 2.9.0、`@minecraft/server-ui` 2.1.0 |
| 交付物 | BP（行为包）+ RP（资源包）+ `build.ps1` 打包脚本 + `.mcaddon` 成品 + 文档（README/使用手册/交接报告） |
| 玩家规模 | 红蓝两队对抗（人数不限，自动分队） |
| 单局时长 | 比赛模式 20 分钟，或先到 2000 分提前结束 |

### 1.2 一句话设计

每个玩家从**四大学派**中选择 1 个（武器 + 被动 + 2 个技能），从**九本圣典**中自选 2 本，组成固定快捷栏负载，在红蓝两队对抗的**夺点竞技场**中使用技能、配合队友、争夺 5 个战略点，以击杀与占领积分决出胜负。

### 1.3 世界观与背景故事

自“第二次东熙之争”的号角吹响，大陆诸国早已破败不堪。在南，“玉玺之变”致使千年古国在朝野动荡的阴影中苟延残喘；在北，民族觉醒的独立运动在联盟的领导下如火如荼。

然而，民联羽翼未丰，革命势寡力薄，面对厉兵秣马的北境雄狮，联盟反倒深陷内斗纠葛，全然无瑕北顾。

彼时熙风国沉疴积久、乱象渐生，亟需一场战事纾解国内矛盾、重振往昔国威。“宣判者”图斯·卡佩洞悉联盟分裂、自顾不暇的绝佳战机，便决意挥师南下，对东陵国正式宣战；两年后，北境大军越过大漠天险，与东陵守军对峙于三河防线，兵锋直指南境腹地。

对熙风而言，民联的分裂终有终结之时，唯有速战速决，方能抓住转瞬即逝的战机。西线峰峦阻隔，关山难越，难以直捣陵都；东线森林密布，沼泽绵延，大军无从展开。唯有中线一带为广袤平原，南北通畅，东西围挡，战略纵深最短，是北境雄兵极速突进、破局制胜的最佳突破口。

扼守河道咽喉的**上密西安**，就此成为两军对峙的核心支点，亦是整场战争的胜负关键之一。这场奠定战争初期格局的惨烈交锋，后世史称——**上密会战**。

（对战背景即两军在此地的决战：红蓝两队分别代表交锋双方，通过争夺要地、压制敌军来奠定战局。）

### 1.4 核心玩法支柱

1. **构筑**：学派 × 圣典的 4×9 组合形成不同战斗风格（爆发、控制、续航、机动、召唤、标记等）。
2. **技能博弈**：所有技能/圣典均为独立物品，手持右键（电脑）/长按（手机）使用，有明确冷却与提示。
3. **团队争夺**：5 个争夺点 + 2 个大本营的占点/接管/回退机制，站位与人数管理决定胜负。
4. **信息对抗**：头顶名牌、普林西斯标记、隐身/蹲下隐藏、幽灵等待等构成侦查与反侦查博弈。
5. **清晰反馈**：粒子轨迹、选区圆环、光柱、侧边栏、浮空字、聊天播报等提供完整视觉反馈。

---

## 2. 游戏设计

### 2.1 玩家构成与角色构建

- 每个玩家固定拥有：**1 件学派武器 + 2 个学派技能物品 + 1 个学派被动（进入学派即生效，无物品）+ 2 本自选圣典**。
- 四大学派：

| 学派 | 定位 | 武器 | 被动 | 技能 I | 技能 II |
|---|---|---|---|---|---|
| 输出丨击灭（jiemie） | 近战爆发/收割 | 战戟 | 勇战 | 突进 | 战愈 |
| 控制丨缚阻（fuzu） | 远程消耗/区域控制 | 法杖 | 无被动 | 迟滞 | 瘴阵 |
| 治疗丨生命（life） | 辅助/续航/保排 | 魔杖 | 自愈 | 同舟 | 祝福 |
| 游走丨疾行（speed） | 机动/切入/侦察 | 长剑 | 无被动 | 掠影 | 隐袭 |

- 九本圣典：布德宗、卡鲁、南十字、塔莫琳、芙希、阿玛拉加、茶雅、普林西斯、佩莉（详见 4.2）。
- 快捷栏固定布局（详见 4.3），物品槽位锁定，不可移动、不可丢弃、不可合成。

### 2.2 全局规则

| 规则 | 说明 |
|---|---|
| 生命提升 V | 所有玩家常驻生命提升 \*4（amplifier 4 = V 级，+10 颗心，总上限 40 点）；复活时重新施加并回满血 |
| 祝福临时提升 | 祝福期间生命提升 \*7（VIII 级，+16 颗心），结束后无缝回落到 V |
| 死亡不掉落 | 发放的 jimie 物品与不死图腾 `keepOnDeath = true` |
| 物品锁定 | 发放物品 `ItemLockMode.slot`（固定槽位，不可移动/丢弃/合成）；重置/回收时先解锁再操作 |
| 友伤 | 同队玩家互相攻击、友方狼咬同队玩家、友方攻击己方狼一律无效（`beforeEvents.entityHurt` 拦截）；自己打自己不拦截（保留同舟/阿玛拉加自伤机制） |
| 队伍 | 红队 tag `jimie_red`、蓝队 tag `jimie_blue`，按进服顺序自动交替分配；聊天 `!team red/blue`（事件可用时）或 `/scriptevent jimie:team` 切换 |
| 盔甲 | 仅作分队标识使用，无防护作用（原需求“盔甲先不用管”，当前不实现） |
| 触发模式 | 和平 / 创造 / 旁观模式均可触发技能与圣典，方便测试 |
| 效果等级约定 | 文档中“效果\*N”= amplifier N = N+1 级（\*0 = I 级、\*1 = II 级……） |
| 伤害归属 | 技能伤害以施法者为伤害来源；法杖/魔杖通过脚本记录击杀归属；狼击杀归属召唤者；环境死亡（摔死/烧死等）不计入被动击杀 |

### 2.3 数值换算约定

- 1 点生命值 = 0.5 颗心；玩家基础生命 20 点（10 颗心）。
- 生命提升 V（amplifier 4）：+20 点（10 颗心）→ 总 40 点。
- 生命提升 VIII（amplifier 7）：+32 点（16 颗心）→ 总 52 点。
- 1 秒 = 20 tick；技能/效果时长一律用 tick 表示（如 5s = 100 tick）。
- 瞬间伤害 \*0 = 6 点魔法伤害（3 颗心）；瞬间治疗 \*1 = 治疗 II（6 点）、\*2 = 治疗 III（8 点）。

### 2.4 数值平衡基线（已实装版本）

- 远程普攻（法杖 2 点 / 魔杖 1 点）CD 0.6s，无默认击退，5 格内轻微击退。
- 技能 CD 区间 18s～90s：迟滞 55s、瘴阵 60s、同舟 50s、祝福 60s、突进 40s、战愈 50s、掠影 35s、隐袭 60s、茶雅 18s、卡鲁 55s、南十字 45s、塔莫琳 45s、芙希 65s、阿玛拉加 20s、普林西斯 70s、佩莉 90s、布德宗图腾 120s。
- 伤害基线：战戟 +2（合计 3 点）、长剑 +1（合计 2 点）、突进 6 点、同舟自伤 6 点、阿玛拉加自伤 6 点、狼咬 4 点（原版近战）。


---

## 3. 游戏流程

### 3.1 总体状态机

```text
世界加载 / 脚本启动（system.run(startup) 延迟 1 tick）
  ├─ 启动扫描：清理残留浮空字/旧方案实体，恢复指示物、狗、阵眼
  ├─ 对在线玩家：补队伍、补名牌、补全局增益、补发物品
  └─ 注册 10 tick 主循环（维护/指示物/狗/阵眼/图腾/比赛/死亡等待/名牌）

玩家进入
  ├─ 首次进入：单次 ModalForm（队伍 + 学派 + 圣典1 + 圣典2，一次提交）
  │    ├─ 取消 → 1s 后自动重弹；60s 未完成 → 默认配置（击灭+布德宗+卡鲁）
  │    └─ 提交 → 写动态属性 → 按槽位发放 5 件物品 + 布德宗副手图腾 → 全局增益
  ├─ 已有学派缺圣典：补选圣典弹窗（2 个下拉框）
  └─ 已有职业无队标：自动补分队

常驻战斗循环（每 10 tick）
  ├─ 全局维护：生命提升 V / 生命学派自愈 / 物品锁定与补发 / 名牌 / 冷却显示
  ├─ 指示物：范围效果刷新、粒子、到期清理
  ├─ 狗：35s 寿命、驯服校验、防偷狗、名牌、击杀归属
  ├─ 佩莉：受击计数、死亡光柱
  ├─ 布德宗：图腾触发 → 恢复13s → 120s 补发
  └─ 比赛：占领/接管/回退/计分/显示

死亡与复活
  └─ 死亡 → 对方 +5 分 → 12s 幽灵（收物品 → 发复活选择器 → 选复活点 → 倒计时）
       → 复活：回满血 + 重发物品 + 强制生存

比赛模式
  ├─ /scriptevent jimie:start → 初始化点位/计分/侧边栏 → 传送大本营 → 强制生存
  ├─ 进行中：占点/接管/回退/锁定、每秒占点分、死亡分、HUD
  ├─ 中途加入：自动观战
  └─ 结束（20 分钟高分胜 / 先到 2000 分 / stop）→ 全服播报 → 全部状态重置
```

### 3.2 阶段一：世界加载与脚本启动

1. 脚本顶层禁止直接调用 `world.getAllPlayers()` 等受限 API（早期执行限制），必须 `system.run(startup)` 延迟到下一 tick 再启动。
2. `startup()` 职责（全部 try/catch 隔离）：
   - 环境探测并输出诊断日志（`[jimie] 环境 primitiveShapes=.. TextPrimitive=..`）；
   - `primitiveShapesManager.removeAll()` 清掉世界重载后残留的浮空字；
   - 扫描恢复运行时实体：`area_indicator`（指示物）、`fuxi_wolf_red/blue`（芙希狗）、`peili_indicator`（佩莉阵眼），从动态属性恢复剩余时间/归属；
   - 清理历史方案残留：旧标记盔甲架（`jimie_marker_for`）、旧末影水晶阵眼（带 `jimie_owner` 的 ender_crystal）、旧原版狼方案残留（带 `jimie_owner` 的 minecraft:wolf）；
   - 对在线玩家：刷新队伍名牌、补队伍、施加全局增益、补物品锁定标记、补发负载；
   - 注册 10 tick 主循环，依次执行 `tickMaintenance / tickIndicators / tickWolves / tickDogLabels / tickPeili / tickTotems / tickGame / tickDeathWait / tickPlayerLabels`。
3. 所有事件订阅带存在性保护：`if (world.afterEvents.xxx) xxx.subscribe(...)`。API 缺失只跳过对应功能，绝不因单个事件不存在而整体崩溃。

### 3.3 阶段二：玩家进入与初始配置

- **首次进入**：弹出「初始配置」单次 `ModalFormData`，包含 4 个下拉框：队伍（红/蓝/自动）、学派（4 选 1）、圣典 1、圣典 2（9 选 1）。一次提交全部配置，避免“连续打开多个表单被客户端吞掉”的问题。
- **提交后**：写入动态属性 `jimie_school / jimie_tome1 / jimie_tome2`，按固定槽位发放 5 件物品；选择布德宗时副手自动发放不死图腾；应用生命提升 V 并回满；刷新队伍名牌。
- **取消**：1 秒后自动重弹；**60 秒内未完成选择**：自动应用默认配置（击灭 + 布德宗 + 卡鲁）并发放物品，保证技能系统可用。
- **补选圣典**：已有学派但缺圣典时，弹补选 ModalForm（2 个下拉框）。
- **补分队**：已有职业但无队标的老玩家自动按人数分配到红/蓝队，不打断流程。
- **防弹窗风暴**：
  - 同一玩家同一时间只允许一个弹窗（`formOpen`）；
  - 弹窗看门狗 120 秒自动清理“客户端吞掉表单导致 promise 不返回”的卡死状态；
  - 重置/切换配置会使旧弹窗结果失效，防止重复提交旧配置。
- **复活**：重新施加生命提升并回满（含 V 的 +10 颗心，总 40 点）、补发全部负载物品、按布德宗 CD 状态恢复副手图腾。

### 3.4 阶段三：战斗进行中（常驻主循环）

每 10 tick（0.5 秒）主循环执行：

| 模块 | 职责 |
|---|---|
| 全局维护 | 生命提升 V 刷新（祝福窗口内为 VIII）；生命学派自愈恢复 I；`keepOnDeath`/`lockMode` 补标记；缺物品补发；缺队标补分队 |
| 指示物 | 范围效果刷新（目标已有同效果不覆盖）；光柱与边界环粒子；到期清理 |
| 芙希狗 | 35 秒寿命倒计时；驯服校验/防偷狗；狗名牌刷新 |
| 佩莉 | 阵眼受击状态；主人死亡后的红色光柱 |
| 布德宗 | 副手图腾“有→无”检测；触发后恢复 II 改 13s；120s 后自动补 |
| 比赛 | 占领/接管/回退/锁定、占点计分、死亡计分、侧边栏/浮空字/粒子 |
| 死亡等待 | 幽灵隐身与无敌、复活倒计时、复活点校验、到点复活 |
| 名牌 | 玩家/狗名牌文字与可见名单刷新 |

**技能/圣典使用流程**（`handleUse`）：

1. 订阅 `world.afterEvents.itemUse`（**不要使用 `itemUseOn`**，2.9.0 已移除该事件；在方块上使用同样触发 itemUse）。
2. 同一 tick 同一物品去重（`lastUse` 表），防止一次操作触发两次。
3. 脚本侧冷却表检查：冷却中拦截；技能/圣典提示剩余秒数，法杖/魔杖静默。
4. 执行对应技能逻辑；无论成功失败，使用后立即写入冷却（符合“使用即开始算、失败也照扣”的设计）。
5. 物品 JSON 原生 `minecraft:cooldown` 与脚本 `ITEM_CD` 数值完全一致，双轨冷却。

### 3.5 阶段四：死亡与复活（12 秒幽灵流程）

1. 任意玩家死亡：**对立队伍 +5 分**（含环境死亡/自杀）。
2. 点击复活（或系统自动）进入 **12 秒（240 tick）幽灵等待**：
   - 幽灵状态：隐身 + 无敌 + 漂浮在所选复活点上方 30 格；不参与点位判定；不向敌方暴露名牌；
   - 自动收回全部 jimie 武器/技能/圣典以及主/副手不死图腾，只保留「复活选择器」（固定第 5 格，槽位锁定）；
   - 「复活选择器」可循环切换复活点：己方大本营（默认）→ 己方已占争夺点 → 佩莉阵眼（仅释放者，用后销毁）；
   - 切换方式：潜行（事件可用时）或使用复活选择器；原选择点失效自动切回大本营；
   - ActionBar 显示“复活等待剩余 X 秒”（≥10s 取整，<10s 保留 1 位小数）。
3. 等待结束：传送至所选复活点（佩莉阵眼上方 2.5 格；大本营/点位 y=-60），移除隐身，回满血，重新发放全部物品，强制生存。

### 3.6 阶段五：夺点比赛

**开始**（`/scriptevent jimie:start`）：
- 比赛已在进行则拒绝并提示；
- 初始化 7 个点位（2 大本营 + 5 争夺点）、比分 0:0、重建计分板、清理旧浮空字与普林西斯标记；
- 所有玩家：移除观战 tag、无队标自动分队、强制生存、传送回己方大本营（y=-60）、施加全局增益、补发物品；
- 全服播报“比赛开始：20分钟 / 先到2000分”。

**进行中**（每 10 tick）：
- 点位判定：半径 17、高度 ±20、主世界、非幽灵、非观战、存活；
- 占领/接管/回退/锁定逻辑（详见 4.5）；
- 计分：占领点位每秒 +1/2/4/8/16；死亡给对立队伍 +5；先到 2000 分立即结束；
- 显示：点位粒子圆环、点位浮空字、侧边栏、站圈 ActionBar。

**中途加入**：比赛进行中首次进服 → 自动进入旁观者模式（`jimie_spectator` tag + `GameMode.spectator`），不参与点位判定；比赛结束时移除。

**结束**（时间到 / 先到 2000 分 / `jimie:stop`）：
- 时间到按高分判胜，平分判平局；先到 2000 分立即判胜；
- 全服播报胜负；
- 重置：比分清零、点位全部中立、清除侧边栏/点位浮空字/普林西斯标记、清除全部幽灵状态与复活选择器、移除观战 tag、强制生存、传送回大本营。

### 3.7 阶段六：测试与重置渠道

- 全部 `/scriptevent` 与聊天命令见 4.6；`jimie:reset` 会清空物品/配置/队伍并重新走完整“队伍 → 学派 → 圣典”流程。
- 测试命令不消耗物品、不影响正式玩法：`jimie:summon` 直接召唤芙希狗，`jimie:give` 按配置补发物品。

---

## 4. 游戏细节

### 4.1 四大学派

#### 4.1.1 输出丨击灭学派（id: jiemie）——近战爆发 / 收割

| 项目 | 内容 |
|---|---|
| 武器 | 击灭丨战戟（`jimie:halberd`）：近战攻击 +2，总攻击 3 点（1.5 颗心）；无 CD |
| 被动 | 击灭丨勇战：最后一击击杀敌对玩家后获得力量 I 5 秒（100 tick）；狼击杀也算；摔死/烧死等环境死亡不算 |
| 技能 I | 击灭丨突进（`jimie:charge`，CD 40s） |
| 技能 II | 击灭丨战愈（`jimie:heal`，CD 50s） |

**突进（技能 I）**：
- 判定范围：施法者面向方向水平 **±20° 扇形、半径 13 格、垂直 ±7.5 格** 的柱体内所有敌对目标（敌对玩家 + 敌方狗）；
- 效果：范围内目标受到 **6 点魔法伤害**（伤害来源=施法者，保证击杀归属）；范围内敌对目标与施法者**统一传送到“施法者面向方向 13 格、上方 3.5 格”的同一点**（传送坐标做安全钳制）；
- 施法者获得 **60 tick 免摔落**（`beforeEvents.entityHurt` 中取消 fall 伤害）；敌人正常摔落；
- 范围内没有敌人也照常位移；冷却照常扣除。

**战愈（技能 II）**：恢复 III 2 秒（40 tick）+ 伤害吸收 III 10 秒（200 tick），仅作用于自己。

#### 4.1.2 控制丨缚阻学派（id: fuzu）——远程消耗 / 区域控制

| 项目 | 内容 |
|---|---|
| 武器 | 缚阻丨法杖（`jimie:wand_bind`）：35 格视线射线，命中第一个敌对目标或实心方块停止；命中造成 **2 点魔法伤害**；CD 0.6s |
| 被动 | 无 |
| 技能 I | 缚阻丨迟滞（`jimie:slow_field`，CD 55s） |
| 技能 II | 缚阻丨瘴阵（`jimie:poison_field`，CD 60s） |

**迟滞（技能 I）**：35 格视线方向放置指示物；以指示物为中心**半径 10 柱体**内敌对目标持续获得**缓慢 II**；指示物存在 **10 秒**（200 tick）。

**瘴阵（技能 II）**：同上，持续**致命中毒 II**（`minecraft:fatal_poison`）；指示物存在 **15 秒**（300 tick）。

#### 4.1.3 治疗丨生命学派（id: life）——辅助 / 续航 / 保排

| 项目 | 内容 |
|---|---|
| 武器 | 生命丨魔杖（`jimie:wand_life`）：同法杖射线，命中造成 **1 点魔法伤害**；CD 0.6s |
| 被动 | 生命丨自愈：常驻生命恢复 I（主循环维护刷新） |
| 技能 I | 生命丨同舟（`jimie:same_boat`，CD 50s） |
| 技能 II | 生命丨祝福（`jimie:bless`，CD 60s） |

**同舟（技能 I）**：
- 施放中心：射线途中先遇友方玩家 → 以该友方脚下为中心；否则以第一个方块精确命中面为中心；35 格内无目标 → 以施法者当前位置为中心；
- 效果：半径 7 柱体内**友方玩家（不含自己）**获得一次**瞬间治疗 II**（6 点）；
- 施法者自己固定受 **6 点魔法伤害**（自伤，周围没有队友也照常受伤）；
- 视觉：绿色 3 帧选区环 + 治疗光柱。

**祝福（技能 II）**：
- 施放中心规则与同舟一致；
- 效果：半径 7 柱体内友方（**含自己，但自己同样必须在半径内**）获得：瞬间治疗 III（8 点）+ **生命提升 VIII 15 秒**（结束后回落到 V）+ **免疫负面 5 秒**；
- 祝福到期无缝过渡：脚本在到期瞬间移除 VIII → 立刻补回 V → 把当前血量写为 `min(到期瞬间血量, 40)`；祝福期间的治疗/受伤全部保留，血量数字不丢、血条不空；
- 免疫窗口内维护固定补 40 tick 的 VIII，避免两个维护周期之间效果自然消失；
- 视觉：金色 3 帧选区环 + 金色光柱。

#### 4.1.4 游走丨疾行学派（id: speed）——机动 / 切入 / 侦察

| 项目 | 内容 |
|---|---|
| 武器 | 疾行丨长剑（`jimie:longsword`）：近战攻击 +1，总攻击 2 点（1 颗心）；无 CD |
| 被动 | 无 |
| 技能 I | 疾行丨掠影（`jimie:dash`，CD 35s） |
| 技能 II | 疾行丨隐袭（`jimie:stealth`，CD 60s） |

**掠影（技能 I）**：沿视线方向**水平高速位移 10 格**；每 0.5 格检测实心方块，**会被墙挡住**，不做垂直滞空；瞬移逐点推进（每 tick 前进 2 步）；前方立即受阻则提示失败（冷却照扣）。

**隐袭（技能 II）**：隐身 15 秒（300 tick）+ 速度 II 15 秒。

### 4.2 九本圣典/法典/秘典

| 圣典 | identifier | 效果 | CD |
|---|---|---|---|
| 禅师丨布德宗圣典 | `jimie:tome_buddha` | 被动：副手自动持有不死图腾；触发后图腾恢复 II 45s 改为 13s；120s 后自动补充 | 被动（图腾 120s） |
| 司铎丨卡鲁教廷圣典 | `jimie:tome_priest` | 8 秒免疫负面（清除已有 + 阻止新增；瞬间伤害不可免疫） | 55s |
| 骑士丨“南十字”法典 | `jimie:tome_knight` | 伤害吸收 III + 抗性提升 I，10 秒 | 45s |
| 医师丨塔莫琳秘典 | `jimie:tome_doctor` | 生命恢复 II，10 秒 | 45s |
| 召唤师丨芙希秘典 | `jimie:tome_summoner` | 召唤 2 只驯服狗，35 秒后消失 | 65s |
| 魔剑士丨阿玛拉加秘典 | `jimie:tome_blade` | 自伤 6 点 + 中毒 II 2 秒 + 力量 I 7 秒；允许把自己毒死 | 20s |
| 药师丨茶雅秘典 | `jimie:tome_herbalist` | 指示物半径 5，持续中毒 I，存在 8 秒 | 18s |
| 预言家丨普林西斯秘典 | `jimie:tome_prophet` | 指示物半径 15，敌对玩家被标记（仅己方可见）+ 破除隐身，存在 25 秒 | 70s |
| 阵法师丨佩莉秘典 | `jimie:tome_formation` | 在脚下最近地面放置阵眼；敌方攻击 3 次破坏；死亡后光柱；可选复活点（当前为自动复活占位实现） | 90s |

**布德宗（被动）**：
- **只有明确选择布德宗（`jimie_tome1/2 === 'buddha'`）的玩家生效**，背包持有圣典但不选择不生效；
- 副手自动持有不死图腾（`keepOnDeath`）；
- `tickTotems` 每 10 tick 检测副手图腾“有→无”：触发后先 `removeEffect(regeneration)` 再给**恢复 II 13 秒（260 tick，amp 1）**；
- 图腾 CD 120 秒（`jimie_totem_remain` 动态属性倒计时）；CD 结束且玩家存活 → 副手自动补充；CD 期间死亡继续计算，复活后 CD 结束自动补；
- 幽灵等待期跳过图腾逻辑（图腾被暂收，复活后按 CD 补回）；
- 手持圣典使用可查询状态（已就绪 / 冷却中 Xs / 缺失）。

**卡鲁（8s 免疫）**：实现三层防护——① 指示物对免疫窗口内目标源头跳过；② `beforeEvents.effectAdd` 直接取消负面；③ `afterEvents.effectAdd` 兜底立即清除。负面匹配同时支持英文 ID / 短名 / 中文名 / `effect.*` 翻译键（该版本 `effectType` 会被本地化）。

**南十字**：吸收 III + 抗性 I，10 秒。

**塔莫琳**：恢复 II，10 秒。

**芙希（召唤狗）**：详见 4.4.8；狼击杀归属召唤者（可触发勇战）；狼算友方单位；35 秒内被提前打死 CD 照常。

**阿玛拉加**：自伤 6 点（`damagingEntity` = 自己）+ 中毒 II 2 秒 + 力量 I 7 秒；允许自伤致死。

**茶雅**：指示物半径 5，中毒 I，存在 8 秒。

**普林西斯（标记 + 破隐）**：详见 4.4.10。

**佩莉（阵眼）**：详见 4.4.9。

### 4.3 物品清单与快捷栏

| identifier | 名称 | CD | 固定槽位 |
|---|---|---|---|
| `jimie:halberd` | 击灭丨战戟 | 无 | 第 5 格 |
| `jimie:longsword` | 疾行丨长剑 | 无 | 第 5 格 |
| `jimie:wand_bind` | 缚阻丨法杖 | 0.6s | 第 5 格 |
| `jimie:wand_life` | 生命丨魔杖 | 0.6s | 第 5 格 |
| `jimie:charge` | 击灭丨突进 | 40s | 第 3 格（技能 I） |
| `jimie:heal` | 击灭丨战愈 | 50s | 第 4 格（技能 II） |
| `jimie:slow_field` | 缚阻丨迟滞 | 55s | 第 3 格 |
| `jimie:poison_field` | 缚阻丨瘴阵 | 60s | 第 4 格 |
| `jimie:same_boat` | 生命丨同舟 | 50s | 第 3 格 |
| `jimie:bless` | 生命丨祝福 | 60s | 第 4 格 |
| `jimie:dash` | 疾行丨掠影 | 35s | 第 3 格 |
| `jimie:stealth` | 疾行丨隐袭 | 60s | 第 4 格 |
| `jimie:tome_buddha` | 禅师丨布德宗圣典 | 被动 | 第 6/7 格 |
| `jimie:tome_priest` | 司铎丨卡鲁教廷圣典 | 55s | 第 6/7 格 |
| `jimie:tome_knight` | 骑士丨“南十字”法典 | 45s | 第 6/7 格 |
| `jimie:tome_doctor` | 医师丨塔莫琳秘典 | 45s | 第 6/7 格 |
| `jimie:tome_summoner` | 召唤师丨芙希秘典 | 65s | 第 6/7 格 |
| `jimie:tome_blade` | 魔剑士丨阿玛拉加秘典 | 20s | 第 6/7 格 |
| `jimie:tome_herbalist` | 药师丨茶雅秘典 | 18s | 第 6/7 格 |
| `jimie:tome_prophet` | 预言家丨普林西斯秘典 | 70s | 第 6/7 格 |
| `jimie:tome_formation` | 阵法师丨佩莉秘典 | 90s | 第 6/7 格 |
| `jimie:respawn_selector` | 复活选择器 | 无 | 幽灵期第 5 格 |

**槽位布局**（快捷栏第 N 格 / 容器索引）：

| 槽位 | 内容 |
|---|---|
| 第 3 格（索引 2） | 学派技能 I |
| 第 4 格（索引 3） | 学派技能 II |
| 第 5 格（索引 4） | 学派武器；幽灵等待期被复活选择器临时占用 |
| 第 6 格（索引 5） | 圣典 1 |
| 第 7 格（索引 6） | 圣典 2 |

**发放规则**（`ensureLoadout`）：
- 先清理背包其他位置的同 id 物品（先解锁再移除），再按槽位发放，设置 `ItemLockMode.slot` + `keepOnDeath`；
- 单件失败不中断整批，失败时聊天提示 + 掉落脚下兜底；
- 主循环定期补打锁定标记，防止玩家操作或版本行为导致锁丢失；
- 重置/切换配置时先解锁再回收全部 jimie 物品。

**物品 JSON 规范**：`format_version: 1.26.40`；`max_stack_size: 1`；`use_modifiers`（use_duration 0.1、movement_modifier 1.0）；技能/圣典带 `minecraft:cooldown`（category 每物品唯一、type=use、数值与脚本 `ITEM_CD` 完全一致）；武器 `hand_equipped: true` + `minecraft:damage`；图标经 RP `item_texture.json` 映射（可引用原版贴图或本地 PNG）。

### 4.4 通用机制

#### 4.4.1 射线（法杖 / 魔杖）

- 起点：玩家头部 `getHeadLocation()`；方向 `getViewDirection()`；最远 **35 格**；步进 **0.25 格**；
- 命中顺序：第一个**敌对目标**（玩家或敌方狗，近点判定）→ **实心方块** → 35 格上限；**友方与普通生物不阻挡射线**；
- 世界边界保护：步进点低于/高于 `heightRange` 立即终止，粒子和终点做钳制（防 `LocationOutOfWorldBoundariesError`）；
- 伤害：`applyDamage(damage, { cause: "magic" })`，**不带 `damagingEntity`**（消除引擎默认攻击击退）；击杀归属写入 `recentHurtBy`，实体死亡时回填施法者；
- 击退：仅目标距施法者 ≤5 格时手动轻微击退（水平 0.55 / 垂直 0.22，方向从施法者指向目标）；
- 视觉：静态轨迹粒子每 0.5 格一粒 + 0.3 秒飞行弹丸（6 tick 插值）。缚阻法杖 = `minecraft:spell` 轨迹 + `endrod` 弹丸；生命魔杖 = `heart_particle` 轨迹 + 爱心弹丸；
- 音效：`mob.blaze.shoot`。

#### 4.4.2 指示物与范围效果（迟滞 / 瘴阵 / 茶雅 / 普林西斯）

- **落点**：射线先遇敌人 → 敌人脚下（1 格吸附）；否则用 `getBlockFromRay` 的 `faceLocation + block.location` 取精确命中面坐标（与准星一致）；
- **35 格内无目标**（对空/超距）：以施法者当前位置为中心释放，冷却照扣，聊天注明“已以自身为中心释放”；
- 实体：`jimie:area_indicator`（隐形 marker：无碰撞、不受伤、`persistent`），动态属性保存 skill/team/ownerId/center/radius/effect/amplifier/remain；
- 判定：**柱体**，水平半径按技能，垂直 ±7.5（总高约 15 格）；
- 施加：每 10 tick 检查 `getEffect()`——**目标已有同效果则不覆盖**（防止毒伤计时器被不断重置）；效果时长 40 tick（2 秒），离开范围约 2 秒内消退；
- 范围目标：敌对玩家 + 敌方狗（按 `jimie_owner` 判定；友方狗不受影响）；免疫窗口内目标被源头跳过；
- 视觉：光柱 + 半径粒子边界环（环高约 1.5 格），颜色随技能区分。

#### 4.4.3 冷却系统

- **双轨**：物品 JSON 原生 `minecraft:cooldown`（显示冷却）+ 脚本 `ITEM_CD` 表强制拦截（该版本原生物品冷却不拦截 itemUse）；
- 使用即开始计算，**失败也照扣**；冷却中再次使用提示剩余秒数（法杖/魔杖完全静默，不提示、不进名牌 CD 行）；
- 名牌 CD 行只统计技能与圣典，不统计武器；
- 重置/切换配置时清空该玩家脚本侧冷却状态。

#### 4.4.4 免疫负面（卡鲁 / 祝福）

- 负面清单：**缓慢、中毒、致命中毒、虚弱、失明、反胃、饥饿、凋零、挖掘疲劳、黑暗、漂浮**；
- **瞬间伤害不可免疫**；免疫 = 清除已有 + 阻止新增；自己给自己上的负面同样被清除/阻止；
- 三层防护：① 指示物源头跳过；② `beforeEvents.effectAdd` 取消（注意 `effectType` 本地化匹配）；③ `afterEvents.effectAdd` 兜底 `removeNegatives`；
- 免疫日志：`[jimie] 免疫拦截 <效果名>` / `[jimie] 免疫兜底清除 <效果名>`。

#### 4.4.5 伤害与击杀归属

- 技能伤害：`applyDamage(数值, { cause: "magic", damagingEntity: 施法者 })`（突进、同舟自伤、阿玛拉加等），击杀归属直接成立；
- 法杖/魔杖：不带 `damagingEntity` + `recentHurtBy` 击杀回填；
- 芙希狗：原版近战伤害 4，实体死亡时按 `jimie_owner` 归属召唤者；
- 勇战被动：仅“最后一击击杀敌对玩家”触发，含狗击杀，不含环境死亡；`entityDie` 中无直接伤害来源时回退查 `recentHurtBy`。

#### 4.4.6 祝福生命提升过渡（补充）

- 祝福生效期间主循环固定补 40 tick 的 VIII，避免窗口空档；
- 到期瞬间：移除 VIII → 补回 V → `setCurrentValue(min(当前血量, 40))`；期间治疗/受伤数据不丢失。

#### 4.4.7 布德宗图腾（补充）

- 仅配置为布德宗生效；图腾放副手、`keepOnDeath`；
- 触发检测：副手“有→无”；触发后移除图腾自带恢复 II 45s，改给恢复 II 13s（260 tick）；
- `jimie_totem_remain`（秒）每 10 tick 递减；归零且副手空 → `equippable.setEquipment(Offhand, totem)`；
- 死亡不重置 CD；复活后 CD 结束自动补；幽灵期跳过。

#### 4.4.8 芙希狗（召唤师秘典）

- 实体：`jimie:fuxi_wolf_red` / `jimie:fuxi_wolf_blue`（format 1.26.10 自定义实体，**不使用 `runtime_identifier: minecraft:wolf`**，否则原版驯服狼 AI 会覆盖主动索敌）；
- 外观：原版狼模型/材质 + 自定义行走动画；项圈颜色红 14 / 蓝 11；
- 基础组件：血量 8（驯服后 20）、近战攻击 4、`sittable`、`float/leap_at_target/melee_attack/random_stroll/look_at_player/hurt_by_target`、移动/导航/跳跃/碰撞原版同款；
- 驯服组件组 `jimie:on_tame`：`is_tamed`、health 20、attack 4、color、`teleport_to_owner`、`owner_hurt_by_target`（优先级 1）、`owner_hurt_target`（优先级 2）、`nearest_attackable_target`（优先级 3：`is_family=player` + `has_tag=对方队伍`，35 格，`must_see=false`，`scan_interval=5`）、`follow_owner`（优先级 6）；
- 基础组件另留同款索敌兜底（优先级 7）；
- 召唤流程：`spawnEntity`（安全坐标）→ `tameable.tame(player)` → `triggerEvent("jimie:on_tame")` → 10 tick 驯服校验，未生效自动补触发（最多 3 次）；写入 `jimie_owner` / `jimie_remain=700` / 队伍 tag；隐藏原版 nameTag；
- 生命周期：**35 秒**到期消失；脚本不造成任何伤害、不干预移动（纯原版 AI 走咬）；被非主人驯服立即移除（防偷狗）；
- 友伤：友方玩家/友方狗攻击己方狗一律取消；狗不攻击主人与友方；
- 受击：敌方法杖/魔杖射线可命中（按主人队伍判定，友方狗穿透）；敌方迟滞/瘴阵/茶雅/突进范围可作用（`enemyWolvesInCylinder`）；狗自带抗性 II（约 40% 减伤）；
- 击杀归属：狗咬死玩家记给召唤者（可触发勇战）；
- 测试注意：**和平模式下狗不会攻击**，需非和平模式验证；
- 诊断日志：`[jimie] 狼召唤` / `驯服结果` / `狼驯服校验` / `狼索敌` / `狗追击检查` / `狗咬伤`。

#### 4.4.9 佩莉阵眼（阵法师秘典）

- 使用：清除自己旧阵眼 → 以脚下最近地面为位置放置阵眼（`getBlockBelow`，不会放到虚空；失败提示且 CD 照扣）；
- 实体 `jimie:peili_indicator`：隐形、碰撞箱 0.8×1.5、`custom_hit_test`（pivot y=0.75）、health 1000、`damage_sensor` 不造成伤害、**不会爆炸**、`persistent`；
- 视觉（纯粒子，不依赖模型）：发光粒子球 + 底部 1 格光环 + 光柱；
- 拆除：`afterEvents.entityHurt` 只统计**敌对玩家**命中，脚本内存表计数 3 次 → `remove()` + 全服播报；每次受击有粒子爆点 + 「阵眼受击 x/3」提示；己方攻击不计数；
- 死亡光柱：主人死亡后阵眼每 10 tick 喷红色光柱（约 24 格高），所有玩家可见；
- 复活：作为可选复活点（仅释放者）；幽灵漂浮阵眼上方 30 格；复活传送到阵眼上方 2.5 格并销毁阵眼；
- 启动扫描恢复阵眼，并清理旧方案（末影水晶）残留。

#### 4.4.10 普林西斯标记（预言家秘典）

- 标记使用 `TextPrimitive`：`attachedTo = 被标记者`（自动绑定跟随），偏移 (0, 2.2, 0)，scale 0.9，半透明黑底，`depthTest=false` 始终显示；
- 可见名单：**施法方全队 + 施法者本人**，每 10 tick 刷新；名单为空时不创建标记（**空数组 = 全员可见，是文档坑，必须避免**）；
- 生命周期：敌人离开半径或指示物到期 → `remove()`；世界重载 `removeAll()` 清残留；`prophets` 表以指示物实体 id 为键；
- **破除隐身**：指示物范围内隐身敌对玩家被 `removeEffect(INVISIBILITY)`（幽灵除外）；
- 兜底：运行时无 `primitiveShapesManager` 时回退盔甲架方案（无 visibleTo 支持，所有玩家可见，需日志标注）。

#### 4.4.11 名牌与信息显示

**玩家名牌**（TextPrimitive，attachedTo=玩家，偏移 (0,2.5,0)，scale 0.6）：
- 行结构：名字（红 §c / 蓝 §b）→ 血量（10 格血条 + 当前/上限；>50% 绿 / >25% 黄 / 其余红，空血格灰色）→ 学派 + 两本秘典（短名）→ 状态（最多 2 行：生命提升 \*4 不显示；生命提升 \*7 显示“生命提升+3”；其余效果按 \*N 显示等级与剩余秒数；另有“免疫 / 免摔落 / 图腾CD”脚本状态）→ CD（正在冷却的技能/秘典剩余秒数，全就绪显示“CD 就绪”）；
- 可见规则：**本人不可见**；同队恒可见；敌方 35 格内可见，但**隐身或蹲下时隐藏**；普林西斯指示物范围内无视隐藏向施法方全队显示；幽灵等待期不向敌方暴露；没有任何观众时名牌隐藏（避免空数组坑）；
- 原版 `nameTag`：TextPrimitive 可用时置空（不可用时回退队伍染色名牌）。

**狗名牌**：`[红队/蓝队] 主人名的狗` + 血量条/数值；偏移 (0,1.15,0)、scale 0.5；可见规则与玩家一致。

#### 4.4.12 粒子、音效与视觉反馈

| 场景 | 表现 |
|---|---|
| 法杖/魔杖 | 静态轨迹粒子 + 飞行弹丸（缚杖蓝/endrod，愈杖爱心）+ blaze 射击音 |
| 指示物（迟滞/瘴阵/茶雅/普林西斯） | 光柱 + 半径粒子边界环（y+1.5，颜色随技能） |
| 同舟 / 祝福 | 施放瞬间 3 帧选区环（绿 / 金）+ 光柱 + 升级音 |
| 突进 | 末影人传送音 + 命中目标暴击粒子 |
| 战愈 | villager_happy 粒子 + 升级音 |
| 佩莉阵眼 | 发光粒子球 + 底部光环 + 光柱；死亡后 24 格红色光柱 |
| 点位 | 半径 17 粒子圆环（y=-53.5），颜色随归属：红/蓝/中立灰 |
| 名牌 | TextPrimitive 浮空字，半透明黑底，始终可见 |

### 4.5 夺点比赛细节

**地图与点位**（世界坐标，主世界）：

| 点位 | 坐标 (x, z) | 说明 |
|---|---|---|
| 红方大本营 | (0, 100) | 不可占领、不计分、默认复活点 |
| 蓝方大本营 | (0, -100) | 不可占领、不计分、默认复活点 |
| 中心点 | (0, 0) | 争夺点 |
| 东北点 | (87, 50) | 争夺点 |
| 西北点 | (-87, 50) | 争夺点 |
| 东南点 | (87, -50) | 争夺点 |
| 西南点 | (-87, -50) | 争夺点 |

- 判定：半径 **17 格**圆、中心 y=-55、高度 ±20（-75 ~ -35）；玩家必须主世界、存活、非幽灵、非观战；
- 重生点 y = -60；点位粒子圆环绘制于 y=-53.5。

**占领规则**（固定速度，与圈内人数无关）：

| 场景 | 规则 |
|---|---|
| 中立点，单方在场 | 20 秒（400 tick）占领；进度 1:1 随在场推进 |
| 中立点，无人 | 进度 1:1 回退到 0 |
| 已占点，敌方单独在场 | 40 秒（800 tick）接管；进度 1:1 推进 |
| 已占点，攻方阵亡/撤出 | **接管进度 1:1 回退到 0**（即使守方留在圈内也要回退；这是防冻结的关键规则） |
| 双方同时在场 | 占领与回退全部暂停（“进度锁定”） |
| 占领/接管完成 | 归属变更、进度清零、全服播报 |

**计分**：
- 任意玩家死亡：对立队伍 **+5 分**（含环境/自杀）；
- 占领点位数量 1/2/3/4/5 → 每秒 +1/2/4/8/16 分（按 10 tick 半分累积）；
- 计分板目标 `jimie_game`：红队、蓝队、剩余时间、5 个争夺点状态行。

**胜负**：
- 先到 **2000 分**立即获胜；
- 20 分钟（24000 tick）结束，高分方获胜，平分平局；
- **没有“占满 5 点即胜”的条件**。

**HUD 与显示**：
- 侧边栏固定行：红队比分、蓝队比分、剩余时间、每个争夺点一行；行名显示归属/被攻/锁定（如「中心点·红方·蓝攻」），分数列为进度秒数（20s/40s 制）；行名只在状态切换时变化；计分板用 `ScoreboardIdentity` 复用更新，禁止用字符串反复 `setScore` 产生假玩家行；状态切换时重建整个计分板清旧行；
- 点位浮空字（TextPrimitive，scale 3.2，0.01s 精度）：点位名 + 归属 + 被攻/锁定 + 进度秒数，所有玩家可见；
- 站圈 ActionBar：显示点位状态与占领速度/锁定；
- 粒子圆环颜色随归属变化（红/蓝/中立灰）。

**中途加入**：比赛进行中进服 → 自动观战（`jimie_spectator` + 旁观者模式），不参与点位判定，结束时移除。

### 4.6 命令与调试渠道

| 命令 | 作用 |
|---|---|
| `/scriptevent jimie:reset` | 清空物品/配置/队伍，重新走“队伍 → 学派 → 圣典”完整流程 |
| `/scriptevent jimie:give` | 按当前配置补发 5 件物品 |
| `/scriptevent jimie:set jiemie,buddha,priest[,red]` | 直接设置学派 + 两本圣典（第 4 项队伍可选 red/blue/auto） |
| `/scriptevent jimie:school life` | 只改学派，保留圣典与队伍 |
| `/scriptevent jimie:tome buddha,priest` | 只改两本圣典，保留学派与队伍 |
| `/scriptevent jimie:team blue` | 只改队伍（red/blue/auto） |
| `/scriptevent jimie:start` | 开始夺点比赛 |
| `/scriptevent jimie:stop` | 结束并重置比赛 |
| `/scriptevent jimie:summon` | 直接测试芙希召唤（不消耗物品） |
| 聊天 `!team red/blue`、`!reset`、`!give` | 事件可用时的聊天版命令（可选） |

学派 ID：`jiemie / fuzu / life / speed`；圣典 ID：`buddha / priest / knight / doctor / summoner / blade / herbalist / prophet / formation`。

---

## 5. 技术开发规范（重建时强制遵循）

### 5.1 目标环境与技术栈

| 项 | 值 |
|---|---|
| 游戏版本 | Minecraft 基岩版 1.26.40 正式版 |
| 行为包 manifest | `format_version: 2`，`min_engine_version: [1, 26, 40]`，含 data + script 两个模块 |
| 脚本依赖 | `@minecraft/server` 2.9.0、`@minecraft/server-ui` 2.1.0 |
| 实体 format_version | `1.26.0 ~ 1.26.10`（以当前 schema 接受为准） |
| 交付 | `build.ps1` 打包 `.mcaddon`（BP + RP 两个顶层目录） |

### 5.2 工程目录结构（重建目标）

```text
SND5 8-14/                        # 项目根目录（源工程）
├─ BP/
│  ├─ manifest.json               # 行为包 + 脚本模块 manifest
│  ├─ scripts/main.js             # 全部玩法逻辑（单文件，约 3000 行，分层组织）
│  ├─ entities/
│  │  ├─ area_indicator.json      # 通用指示物实体（隐形 marker）
│  │  ├─ peili_indicator.json     # 佩莉阵眼实体
│  │  ├─ fuxi_wolf_red.json       # 芙希狗（红队）
│  │  └─ fuxi_wolf_blue.json      # 芙希狗（蓝队）
│  └─ items/                      # 21 个自定义物品 + 复活选择器
│     ├─ halberd.json / longsword.json / wand_bind.json / wand_life.json
│     ├─ charge.json / heal.json / slow_field.json / poison_field.json
│     ├─ same_boat.json / bless.json / dash.json / stealth.json
│     ├─ tome_buddha.json … tome_formation.json（9 本圣典）
│     └─ respawn_selector.json
├─ RP/
│  ├─ manifest.json
│  ├─ texts/                      # zh_CN.lang / en_US.lang / languages.json
│  ├─ textures/item_texture.json  # 物品图标映射
│  ├─ textures/items/             # 本地图标（复活选择器 PNG、图腾隐藏贴图等）
│  ├─ attachables/                # 图腾/复活选择器手持模型（第一人称可见）
│  ├─ render_controllers/         # 手持隐藏控制器、狼渲染控制器
│  ├─ animations/                 # 手持动画、狼行走动画
│  ├─ entity/                     # fuxi_wolf_red/blue 客户端实体
│  └─ models/entity/              # 自定义手持模型（薄片立方体）
├─ build.ps1                      # 打包脚本（必须兼容 pwsh 7）
├─ README.md / 使用手册.md / HANDOVER.md / 交接报告_*.md
└─ SND5技能包.mcaddon             # 打包产物
```

### 5.3 脚本工程规范（main.js）

按以下顺序组织（单文件也可，模块化拆分亦可，但顺序与命名必须统一）：

1. 常量区：效果 ID、负面清单、学派/圣典表、物品 CD 表、槽位常量、比赛常量（半径/时长/分数）、点位坐标；
2. 运行时状态区：所有 `Map/Set` 状态集中声明（冷却、免疫、指示物、狗、阵眼、名牌、弹窗、比赛、幽灵）；
3. 工具函数区：队伍判定、敌对判定、柱体/锥形判定、精确射线落点、安全坐标、粒子工具；
4. 技能/圣典函数区：每个技能一个函数（`skillXxx` / `tomeXxx`），相互独立；
5. 系统函数区：指示物、狗、图腾、阵眼、名牌、维护循环；
6. 事件订阅区：全部订阅集中、带存在性保护；
7. 比赛函数区：点位、计分、幽灵、复活、开始/结束；
8. 启动区：`loadXxx` 扫描 + `startup()` + `system.run(startup)`。

强制规范：
- **每个函数和关键分支都 try/catch 隔离**，单点失败只跳过自身，绝不让整份脚本崩溃或定时器消失；
- 所有诊断输出统一 `console.warn("[jimie] ...")` 前缀，便于从 ContentLog 过滤；
- 动态属性键统一 `jimie_` 前缀（如 `jimie_school`、`jimie_totem_remain`、`jimie_owner`）；
- 禁止在顶层/事件回调里做重活；定时器（`runInterval`/`runTimeout`）统一在 `startup()` 注册；
- 状态清理要完整：玩家离线/重置/比赛结束都要清对应名牌、冷却、免疫、幽灵、弹窗状态；
- 中文消息用 `§a/§c/§e/§6/§7/§d` 等颜色码区分反馈类型。

### 5.4 API 兼容性铁律（已踩坑清单，实现前必读）

以下是在 1.26.40 / `@minecraft/server 2.9.0` 实测过的关键结论，重建时**必须**遵守：

1. **早期执行限制**：顶层不能调用 `world.getAllPlayers()` 等受限 API，会抛 `cannot be used in early execution`。所有启动逻辑放 `system.run(startup)` 延迟 1 tick。
2. **事件订阅存在性保护**：`if (world.afterEvents.xxx) xxx.subscribe(...)`。实测 `world.afterEvents.itemUseOn` 与 `world.beforeEvents.chatSend` **不存在**（文档有但运行时无），缺失时跳过，不能用它们做核心逻辑。
3. **`Dimension.id` 带命名空间**：返回 `"minecraft:overworld"` 而不是 `"overworld"`，所有维度比较必须写全名（或归一化 `minecraft:` 前缀）。
4. **伤害 API**：`Entity.damage()` 已移除，必须用 `Entity.applyDamage()`。带 `cause:"entityAttack"` + `damagingEntity` 的组合在该版本可能静默无效（不扣血但击退正常），远程/狼伤害统一用 `cause:"magic"` + 归属表方案。
5. **回血 API**：`player.setHealth()` 不存在，用 `HealthComponent.setCurrentValue()`；先加生命提升再立刻读 `effectiveMax` 会读到旧上限（20），必须延迟 2 tick 再回满，目标值保底 40。
6. **原生物品冷却不可靠**：`minecraft:cooldown` 不会拦截 `itemUse` 事件，必须脚本侧 `ITEM_CD` + `cooldownUntil` 强制拦截，两侧数值保持一致。
7. **计分板假玩家**：`objective.setScore(字符串, 分数)` 每次调用都会新建假玩家身份。必须从 `getParticipants()` 找到 `ScoreboardIdentity` 后用身份对象更新同一行；行名变化会产生残留旧行，状态切换时重建整个计分板目标。
8. **TextPrimitive 两个坑**：
   - `visibleTo` 为**空数组 = 所有人可见**，可见名单必须永远至少包含 1 人（如施法者/自己）；
   - `prophets` 之类的表键要用**实体 id**（如 `indicatorEnt.id`），不要用数据对象里不存在的字段（曾导致标记清不掉）；
   - 世界重载时 `primitiveShapesManager.removeAll()` 清残留浮空字。
9. **持续效果刷新坑**：指示物每 10 tick 重上中毒会重置毒伤计时器、永远不跳伤害；必须 `getEffect()` 判断目标已有同效果就不覆盖，让效果自然走完。
10. **before 事件限制**：`beforeEvents` 里写动态属性/发消息可能受限，需要延迟的操作放 `system.run` 下一 tick。
11. **effectAdd 本地化**：`EffectAddAfterEvent.effectType` 会按服务器语言返回本地名（中文服返回“中毒”），负面匹配必须同时支持英文 ID、短名、中文名、`effect.*` 翻译键。
12. **表单链不可靠**：基岩版会吞掉“短时间内连续打开的第二个表单”。配置流程必须用**单个 ModalFormData**（4 个下拉框一次提交），并配 120 秒看门狗与防重弹。
13. **实体 schema 注意**：
    - `minecraft:behavior.look_at_player` 用 `look_distance`（`target_distance` 会解析失败）；
    - `minecraft:behavior.sit` 在该版本不存在（会导致整个实体加载失败）；
    - `runtime_identifier: minecraft:wolf` 会让原版驯服狼 AI 接管行为（主动索敌失效），自定义狗不能用；
    - 末影水晶被攻击会原生爆炸且无法拦截，佩莉阵眼必须用自定义隐形实体 + 纯粒子视觉；
    - 自定义客户端模型（`geometry.*`）在该版本加载不稳定（`geometry not found`），能不用就不用，视觉优先用粒子/TextPrimitive/原版模型。
14. **和平模式**：和平模式下狗不会主动攻击，测试必须在非和平模式进行。
15. **完全重启**：BP/RP 文件改动必须完全退出游戏重进才生效，`/reload` 不重载脚本与 RP。

### 5.5 实体与物品 JSON 规范

- **物品**：`format_version: "1.26.40"`；`minecraft:item` 结构；`menu_category`（武器 equipment / 技能圣典 items）；`minecraft:icon` 引用 RP `item_texture.json` 的键；`minecraft:display_name` 用 `item.jimie:*` 翻译键（zh_CN / en_US 双语）；`minecraft:cooldown` 的 `category` 每物品唯一（如 `jimie_charge`）。
- **指示物实体**（area_indicator）：`is_spawnable: false`、`is_summonable: true`；`physics` 无重力无碰撞；`custom_hit_test` 零尺寸；`damage_sensor` 不造成伤害；`pushable` 不可推；`collision_box` 近似 0；`persistent` 防卸载。
- **佩莉阵眼**：`custom_hit_test` 0.8×1.5（pivot y=0.75）保证近战命中；health 1000；不可爆炸；persistent。
- **芙希狗**：自定义实体（无 runtime_identifier）；基础索敌 + 驯服组件组索敌双份；`nearest_attackable_target` 过滤器 `all_of(is_family=player, has_tag=对方队伍)`，`must_see=false`、`scan_interval=5`、`within_radius=35`。
- **RP 资源**：物品图标引用原版贴图或本地 PNG；复活选择器必须提供本地图标（原版 compass 引用在该版本不稳定）；图腾/复活选择器 attachable 用 `context.is_first_person` 控制第三人称隐藏。

### 5.6 校验、打包、同步与发布流程

**校验**（开发机 PowerShell 执行）：

```powershell
# JS 语法
node --check "C:\Users\24827\SND5 8-14\BP\scripts\main.js"

# 全部 JSON
Get-ChildItem -Path "C:\Users\24827\SND5 8-14" -Recurse -Filter *.json | ForEach-Object {
  try { Get-Content -LiteralPath $_.FullName -Raw | ConvertFrom-Json | Out-Null; Write-Host "OK   $($_.FullName)" }
  catch { Write-Host "FAIL $($_.FullName): $($_.Exception.Message)" }
}
```

**打包**（必须用 pwsh 7，Windows PowerShell 5.1 会按 GBK 读 UTF-8 脚本导致乱码）：

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File "C:\Users\24827\SND5 8-14\build.ps1"
```

`build.ps1` 规范：临时目录 `_build` 内分别放 `jimie_schools_BP` / `jimie_schools_RP` → `Compress-Archive` 成 `_build.zip` → 改名 `SND5技能包.mcaddon`（PowerShell 7 的 Compress-Archive 不接受 .mcaddon 扩展名）。

**同步到已安装目录**（每次修改后必须全部同步，共 6 个位置）：

```powershell
# Shared（全局）
Copy-Item -Path "C:\Users\24827\SND5 8-14\BP\*" -Destination "C:\Users\24827\AppData\Roaming\Minecraft Bedrock\Users\Shared\games\com.mojang\behavior_packs\jimie_schools_BP\" -Recurse -Force
Copy-Item -Path "C:\Users\24827\SND5 8-14\RP\*" -Destination "C:\Users\24827\AppData\Roaming\Minecraft Bedrock\Users\Shared\games\com.mojang\resource_packs\jimie_schools_RP\" -Recurse -Force
# 测试世界内嵌副本（BP/RP 各 2 个世界，路径按实际存档替换）
Copy-Item -Path "C:\Users\24827\SND5 8-14\BP\*" -Destination "...\minecraftWorlds\<世界ID>\behavior_packs\jimie_schools_BP\" -Recurse -Force
```

**测试铁律**：
- 修改后**完全退出游戏重进**（`/reload` 不生效）；
- 日志：`C:\Users\24827\AppData\Roaming\Minecraft Bedrock\logs\ContentLog*.txt`，先看最新文件里的 `[jimie]` 诊断行；
- 修复后同步 6 个目录 → 重新打包 → 用户完全重启游戏复测。

### 5.7 文档维护规范

- 项目根目录常驻：`README.md`（简介）、`使用手册.md`（玩家可见规则与命令）、`HANDOVER.md`（逐轮修复流水账）、`交接报告_YYYY-MM-DD.md`（每日状态交接）；
- 每次功能/数值改动：更新 HANDOVER.md（新轮次）+ 当日交接报告 + 必要时使用手册；
- 本文档（SND5游戏开发文档.md）是**重建基准**：任何最终确认的设计变更都要回写本文档，保持“文档 = 提示词 = 最终规格”。

---

## 6. 验收标准与回归清单

### 6.1 开局与配置

- [ ] 首次进服弹出单次 ModalForm（队伍/学派/圣典1/圣典2），一次提交成功；
- [ ] 取消弹窗 1s 自动重弹；60s 未完成自动应用默认配置（击灭+布德宗+卡鲁）并发放物品；
- [ ] 提交后 5 件物品按固定槽位发放（第 3~7 格），锁定不可移动/丢弃；
- [ ] 选择布德宗时副手自动持有不死图腾；
- [ ] 所有玩家生命提升 V 且满血（40 点）；复活后同样回满；
- [ ] 自动分队生效（红/蓝交替）；老玩家无队标自动补分；
- [ ] 死亡不掉落；`!team` / `/scriptevent jimie:*` 调试命令全部可用。

### 6.2 四大学派全量

- [ ] 击灭：战戟近战 +2；勇战最后一击力量 I 5s（狼击杀算、环境死亡不算）；突进 ±20°/13 格/6 伤/群传/自身免摔落 3s；战愈恢复 III 2s + 吸收 III 10s；
- [ ] 缚阻：法杖 2 伤/0.6s CD、第一目标命中停止、穿过友方；迟滞缓慢 II 10s；瘴阵致命中毒 II 15s；
- [ ] 生命：魔杖 1 伤/0.6s CD；自愈恢复 I 常驻；同舟友方治疗 II + 自伤 6 点（无队友也受伤）；祝福治疗 III + 生命提升 VIII 15s + 免疫 5s，到期血量无缝过渡不空血条；
- [ ] 疾行：长剑 +1；掠影水平 10 格、墙挡、不滞空；隐袭隐身+速度 II 15s。

### 6.3 九本圣典全量

- [ ] 布德宗：图腾触发 → 恢复 II 13s → 120s 自动补；死亡不重置 CD；复活后按 CD 补；
- [ ] 卡鲁：8s 免疫持续有效（站在迟滞/瘴阵/茶雅内不掉负面；日志出现“免疫拦截/兜底清除”）；瞬间伤害不可免疫；
- [ ] 南十字 / 塔莫琳：数值与时长正确；
- [ ] 芙希：非和平模式召唤 2 只狗 → 35 格索敌 → 原版走咬 4 伤 → 不打友方 → 跟随主人 → 35s 消失 → 击杀归属召唤者；被非主人驯服会被移除；
- [ ] 阿玛拉加：自伤 + 中毒 II 2s + 力量 I 7s，可自伤致死；
- [ ] 茶雅：半径 5 中毒 I 8s，毒伤计时器不被刷新（能跳伤害）；
- [ ] 普林西斯：标记仅施法方可见、绑定被标记者、离开半径/到期消失；范围内隐身敌人被破隐；
- [ ] 佩莉：阵眼可见（粒子）、不会爆炸、敌方 3 次攻击拆除（受击提示 x/3）、主人死亡后红色光柱、阵眼复活 +2.5 格并销毁、旧阵眼被新阵眼替换。

### 6.4 通用机制

- [ ] 法杖/魔杖击杀归属正确（触发勇战）；5 格内轻微击退、远程不击飞；
- [ ] 指示物落点与准星一致（精确命中面）；35 格内无目标时以自身为中心释放且 CD 照扣；
- [ ] 离开指示物范围约 2s 内效果消退；
- [ ] 免疫窗口内自己给自己上的负面也被清除；
- [ ] 同队互伤无效（近战/远程/狗）；同舟/阿玛拉加自伤保留；
- [ ] 名牌：自己不可见、同队可见、敌方 35 格内可见（隐身/蹲下隐藏）、普林西斯范围内无视隐藏、幽灵不暴露、CD 与状态行正确；
- [ ] 复活选择器第一人称可见、第三人称隐藏；幽灵期锁定第 5 格。

### 6.5 夺点比赛

- [ ] `/scriptevent jimie:start` 初始化 7 点位、比分清零、全员传送大本营、强制生存；
- [ ] 中立点 20s 占领；已占点敌方 40s 接管；双方在场锁定；攻方阵亡/撤出后接管进度 1:1 回退（守方留圈也回退）；
- [ ] 死亡给对立队伍 +5（含环境/自杀）；占点每秒 +1/2/4/8/16；
- [ ] 20 分钟到点高分胜/平局；先到 2000 分立即结束；结束全部状态重置；
- [ ] 中途进服自动观战；`jimie:stop` 正常重置；
- [ ] 侧边栏固定行数不刷屏；点位浮空字实时准确；站圈 ActionBar 正确；
- [ ] 12s 幽灵：收物品/发选择器/切换复活点/倒计时/满血复活/物品补发。

### 6.6 技术验收

- [ ] `node --check` 通过；全部 JSON `ConvertFrom-Json` 通过；
- [ ] 加载后 ContentLog 无脚本错误；`[jimie] 环境` 等启动日志正常；
- [ ] 实体无 `failed to load from JSON` / `Multiple manifests` / `geometry not found` 类报错；
- [ ] `build.ps1` 成功产出 `.mcaddon`，导入后 BP/RP 同时生效；
- [ ] 完整回归后更新 HANDOVER / 交接报告 / 使用手册。

---

## 7. 未决事项与后续规划

以下为当前已记录、但尚未最终拍板或未来可扩展的事项（重建时按括号内的现状实现即可，不阻塞交付）：

1. **复活选择 UI**：佩莉阵眼当前为“死后自动在阵眼复活”的占位实现（选择 UI 用户暂缓）；重建保持占位方案，架构上预留可选复活点列表（`respawnOptions` 已支持大本营/己方点/阵眼三类）。
2. **不死图腾第三人称隐藏**：1.26.40 中自定义 attachable 无法可靠覆盖原版图腾的手持渲染（动画/渲染控制器/透明贴图分离均实测无效，用户已拍板放弃）。复活选择器（自定义物品）的第三人称隐藏可正常生效。若未来要隐藏图腾，唯一方案是覆盖 `textures/items/totem.png` 为全透明（会连第一人称一起隐藏），需用户确认后再做。
3. **点位数值**：点位中心 y=-55、重生 y=-60、判定高度 -75~-35、半径 17 为当前约定值，可随地图美术调整；修改时同步更新点位浮空字、粒子环与重生逻辑。
4. **芙希狗最终表现**：自定义驯服狗 + 主动索敌方案为当前确认方案；若 `has_tag` 过滤器在实机仍不稳定（狗“基本不动”），备选是脚本追击助推（用户曾否决，需重新确认后才能采用）。
5. **盔甲系统**：需求中“盔甲只用于分队标识、无防护作用”尚未实现（用户此前说“先不用管”），留作后续扩展。
6. **比赛与技能清场**：比赛开始/结束当前不清理通用技能指示物（迟滞/瘴阵/茶雅/普林西斯）；如希望开赛统一清场，需在 `startGame/endGame` 增加清理逻辑。
7. **比分板格式**：剩余时间显示格式、点位行名配色（红 §c/浅、蓝 §b/浅、深色 §4/§9、中立灰）可按 UI 需求微调，但必须保持“固定行 + 身份复用 + 状态切换重建”的实现原则。

---

## 8. 附录：关键常量速查

### 8.1 效果与负面清单

| 用途 | 值 |
|---|---|
| 常驻生命提升 | `minecraft:health_boost`，amplifier 4（V） |
| 祝福生命提升 | `minecraft:health_boost`，amplifier 7（VIII） |
| 负面清单 | slowness、poison、fatal_poison、weakness、blindness、nausea、hunger、wither、mining_fatigue、darkness、levitation |
| 不可免疫 | 瞬间伤害（instant_damage） |

### 8.2 时间换算

| 值 | tick |
|---|---|
| 1 秒 | 20 |
| 生命提升 V 维护 | 每 10 tick 补 40 tick |
| 突进免摔落 | 60 |
| 战愈恢复/吸收 | 40 / 200 |
| 迟滞/瘴阵/茶雅效果刷新 | 每 10 tick 检查，40 tick 持续 |
| 卡鲁免疫 | 160（8s） |
| 祝福免疫 | 100（5s） |
| 祝福生命提升 | 300（15s） |
| 隐袭 | 300（15s） |
| 南十字/塔莫琳 | 200（10s） |
| 阿玛拉加中毒/力量 | 40 / 140 |
| 布德宗恢复 | 260（13s） |
| 布德宗图腾 CD | 2400（120s） |
| 狗寿命 | 700（35s） |
| 幽灵等待 | 240（12s） |
| 中立占领 / 接管 | 400 / 800 |
| 比赛时长 | 24000（20 分钟） |

### 8.3 比赛常量

| 常量 | 值 |
|---|---|
| CAPTURE_RADIUS | 17 |
| ZONE_HALF_HEIGHT | 20 |
| 点位中心 y | -55 |
| 重生 y | -60 |
| WIN_SCORE | 2000 |
| KILL_SCORE | 5 |
| SCORE_PER_NODE | [0, 1, 2, 4, 8, 16] |
| 观战 tag | jimie_spectator |

### 8.4 槽位与标识

| 项 | 值 |
|---|---|
| 快捷栏槽位 | 技能 I=索引 2、技能 II=索引 3、武器=索引 4、圣典 1=索引 5、圣典 2=索引 6 |
| 幽灵期 | 复活选择器=索引 4 |
| 队伍 tag | jimie_red / jimie_blue |
| 学派 ID | jiemie / fuzu / life / speed |
| 圣典 ID | buddha / priest / knight / doctor / summoner / blade / herbalist / prophet / formation |
| 命名空间 | jimie:（物品/实体标识前缀，可全局替换但必须一致） |

### 8.5 运行时状态键（动态属性）

| 键 | 用途 |
|---|---|
| jimie_school / jimie_tome1 / jimie_tome2 | 玩家配置 |
| jimie_team | 队伍（可选，队伍以 tag 为准） |
| jimie_totem_remain | 布德宗图腾 CD 剩余秒 |
| jimie_skill / jimie_team / jimie_owner / jimie_center / jimie_radius / jimie_effect / jimie_amplifier / jimie_remain | 指示物状态 |
| jimie_owner / jimie_hits | 佩莉阵眼归属与受击（受击以脚本内存表为准） |
| jimie_owner / jimie_remain | 芙希狗归属与剩余寿命 |

---

## 9. 附：重建任务书（给开发环境的一句话摘要）

“请按《SND5游戏开发文档.md》从零重建《剑与消亡V：上密会战》基岩版 1.26.40 学派战技能包：实现四大学派（武器+被动+2 技能）、九本圣典、全局规则（生命提升 V、复活满血、死亡不掉落、友伤拦截）、固定快捷栏、头顶名牌、粒子反馈、12 秒幽灵复活流程、5 点夺点比赛（20 分钟/2000 分）、全套 `/scriptevent` 调试命令，并严格遵守第 5 章技术规范（API 兼容性铁律、防御式脚本、双轨冷却、TextPrimitive 与计分板避坑、build.ps1 打包、6 目录同步、ContentLog 日志规范）。完成后按第 6 章验收清单全量回归并更新文档。”
